# 트랙 A 작업 인계 — 2026-09-08

> 후속 구현: 현재 저장소 `track_a/`에 수정 코드·회귀 테스트·vast.ai 실행 안내를 준비했다.
> 아래 내용은 최초 인계 당시 상태다. 최신 구현 및 검증 결과는 `track_a/README.md`를 따른다.

사용자가 제공한 마지막 Claude 세션 기록과 로컬 파일을 대조했다.
현재 이어갈 목표는 실스캔 기반 2.5D 결함 합성이다.
이 저장소의 기존 HANDOFF.md에 있는 TRELLIS.2 GPU 재시도는 현재 우선 작업이 아니다.

## 실제 작업 위치

- 코드: `C:/Users/biop9/zdf-synth`
- 원본 및 캐시: 위 폴더의 `data/2-2.zdf`, `data/0023.zdf`, 각각의 `*_raw.npz`
- 상위 프로젝트: `C:/Users/biop9/bumper-synth`
- 세션 원문: `C:/Users/biop9/OneDrive/Desktop/트랙 A — 2.5D 주입 (빠름, 1~2일).txt`

현재 세션의 쓰기 허용 범위는 trellis2-img2pcd이다. zdf-synth 원본은 수정하지 않았다.

## 파일로 확인한 진행 상태

- `zdf_io.py`: ZDF 로딩, organized xyz/RGBA/SNR/normal NPZ 캐시.
- `preview.py`, `partmask.py`: 미리보기 및 부품 마스크.
- `inject.py`: 코사인 벨 찍힘, 선분 긁힘, 법선 재계산, 램버트 음영, 라벨 출력 및 선택적 PCD 출력 코드.
- `parts.yaml`: 범퍼와 브래킷 설정. 범퍼는 above, 브래킷은 near 마스크.
- `measure.py`: 깊이 잔차·법선 노이즈·실물 찍힘 계측.
- 기존 산출물: `out/synth/bumper_cover/0000`~`0002`에 RGB/depth/normal/seg/inst/YOLO/결함 JSON/미리보기 존재. 이번 세션에서 재생성한 것은 아니다.
- `compare.py`: 작성됐지만 엔진과 인터페이스가 불일치한다. `out/compare`는 확인되지 않았다.

기존 `out/measure/2-2/profile.json`의 범퍼 계측 결과:

| 항목 | 값 |
|---|---|
| 점 간격 | 0.1498 mm |
| 유효점 비율 | 0.7468 |
| 평면 잔차 RMS | 0.1341 mm |
| 국소 2차 곡면 잔차 RMS 중앙값 | 0.016 mm |
| SDK 법선 노이즈 std | 2.048도 |
| step 3 법선 노이즈 std | 2.114도 |
| 실물 찍힘 중심 (행, 열) | (559, 1114) |
| 최대 깊이 | 3.059 mm |
| 절반 깊이에서의 반경 | 11.27 mm |
| 배경 적합 고리 잔차 RMS | 0.2967 mm |

절반 깊이 반경과 합성 함수의 0 교차 반경은 서로 다른 정의다. 그대로 대입하지 않는다.
이 값들은 기존 산출물에서 읽었으며 이번 세션에서 재계측하지 않았다.

## 중단 지점과 확인된 불일치

마지막 세션은 림·Blinn-Phong 정반사·실측 범위 반영을 예고한 뒤 compare.py를 작성하다 끝났다.
실제 inject.py에는 해당 개선이 아직 반영되지 않았다.

1. `compare.py`는 `dent_field(..., args.rim)`을 호출하지만 실제 함수에는 rim 인자가 없다.
2. `compare.py`가 호출하는 `inject.shade_ratio()`가 없다.
3. `compare.py`는 `scan['light']`를 사용하지만 `prepare_scan()`은 `light_dir`를 반환한다.
4. 비교 코드의 설명에는 깊이 프로파일 상관이 있으나 JSON에는 그 계산이 없다.
5. 비교 normal 통계는 무효 법선을 0으로 바꿔 각도 표준편차에 포함하므로 유효 픽셀 필터 검토가 필요하다.

현재 셸에서 `python`을 찾지 못했고 `py -0p`는 `No installed Pythons found!`를 반환했다.
따라서 비교 실행과 수치 검증은 수행하지 못했다. 별도 가상환경의 존재 여부는 미확인이다.

## 이어갈 순서

1. zdf-synth를 작업 루트로 설정하고 기존 Python/가상환경 위치 확인, 필요한 의존성 확인.
2. 엔진에 림 및 공통 음영 함수를 구현하고 compare.py와 호출 규약 통일.
3. 실물 중심 (559, 1114), 합성 위치 후보 (760, 1560)에서 비교 실행. 후보의 부품 포함·유효점 여부도 확인.
4. RGB/법선/깊이 단면을 시각 검증하고 유효점 기반 통계를 저장. 합성 유사성이 입증됐다고 미리 판정하지 않는다.
5. 작은 샘플 배치와 PCD 되읽기로 점 수·라벨 정합·결측 보존 확인.
6. 기존 실물 결함의 제외 또는 라벨링 정책을 정리한 뒤 학습용 배치 생성.

추가 검토: 새 결함으로 생기는 센서 결측은 현재 모델링하지 않으며, 원래 스캔의 결측을 유지한다.
격자 점 간격만으로 검출 가능한 최소 깊이를 단정하지 않는다. 기존 설정 주석의 0.5 mm 한계는 검증이 필요하다.

이번 인계에서는 기존 코드·데이터 수정, 설치, 커밋 또는 원격 작업을 수행하지 않았다.
